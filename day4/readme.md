//Topic one is if else
 // Practice Qs
    //print the largest of 2 numbers.
    //two cdonditions are checking in using if else 
    // we can write to conditions without else but if both condition is true the both will be exicuted
    // tax is calculated by income 
    // prient the largest of three

    6 Ternary Operator
    variable = condition ? statement1 : statement2;

    7 Switch statement
    switch (day) {
        case 1: cout << "Monday";
        break;
        case 2: cout << "Tuesday;
        break;
        default: cout<< "Invalid"";
    }
    8 make a calculater
    Question 6 :
typedef keyword in C++ is used for aliasing existing data types, user-defined data
types, and pointers to a more meaningful name.
Eg : typedef int myInt
This allows us to declare new integers using myInt, instead of int.

Macros can be of any type. Macros can even be any code block containing
statements, loops, function calls etc. They are expanded by the preprocessor before
compilation takes place.

const keyword is used to define the constant value that cannot change during
program execution